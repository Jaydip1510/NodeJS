<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="../css/index.css">
    <title>movie</title>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-4 p-5 bg-secondary">
                <form action="/savedata" method="post" name="frm" enctype="multipart/form-data">
                    <input type="hidden" name="hid" value="<%= (user.id!='')?user.id:''%>">
                    <label for="">Movie Name:-</label>
                    <input type="text" name="name"><br><br>

                    <label for="">Release Date</label>
                    <input type="date" name="date" value="<%= (user.id!='')?user.id:''%>?user.date:'' %>"><br><br>

                    <label for="">Chractors</label>
                    <input type="number" name="charactor"
                        value="<%= (user.id!='')?user.id:''%>?user.charactor:'' %>"><br><br>

                    <label for="">Poster Image:-</label>
                    <input type="file" name="image"><br><br>

                    <input type="submit" value="save">

                </form>
            </div>
            <div class="col-8 p-5 bg-warning  d-flex justify-content-center align-items-center">

                <br><br>

                <table border="2" class="table table-striped">
                    <tr align="center">

                        <td>id</td>
                        <td>Name</td>
                        <td>Age</td>
                        <td>Email</td>
                        <td>Image</td>
                        <td>Action</td>

                    </tr align="center">
                    <% for(let i in data){ %>
                        <tr>
                            <td>
                                <%= data[i]._id %>
                            </td>
                            <td>
                                <%= data[i].moviename %>
                            </td>
                            <td>
                                <%= data[i].date %>
                            </td>
                            <td>
                                <%= data[i].charactor %>
                            </td>

                            <td><img src="../<%= data[i].pimage %>" height="100px" width="200px"></td>
                            <td><a href="/edit/<%= data[i]._id%>">Edit</a> <a href="/del/<%= data[i]._id%>">Delete</a>
                            </td>



                        </tr>
                        <%} %>
                </table>
            </div>
        </div>
    </div>
</body>

</html>